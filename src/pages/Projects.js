import React from 'react';
import ProjectList from "../components/ProjectList";

const Projects = () => {
    return (
        <div>

            <ProjectList/>
        </div>
    );
};

export default Projects;